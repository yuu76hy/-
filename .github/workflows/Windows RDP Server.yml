name: Windows RDP Server (Tailscale + 5-digit Password)
on:
  workflow_dispatch:

jobs:
  windows-rdp:
    runs-on: windows-latest
    timeout-minutes: 300
    env:
      RDP_USER: vum
      RUN_DURATION: 18000
      # 手动指定Tailscale默认安装路径（解决环境变量问题）
      TAILSCALE_PATH: "C:\\Program Files\\Tailscale\\tailscale.exe"

    steps:
      - name: Enable RDP Service
        shell: pwsh
        run: |
          Write-Host "=== Enabling RDP Service ==="
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name fDenyTSConnections -Value 0
          New-NetFirewallRule -DisplayName "Allow RDP" -Direction Inbound -LocalPort 3389 -Protocol TCP -Action Allow
          Restart-Service TermService -Force -ErrorAction SilentlyContinue
          Write-Host "✅ RDP Service enabled successfully"

      - name: Create RDP User (5-digit Password)
        shell: pwsh
        run: |
          Write-Host "=== Creating RDP User ==="
          $password = Get-Random -Minimum 10000 -Maximum 99999
          echo "RDP_PWD=$password" | Out-File -FilePath $env:GITHUB_ENV -Append
          
          $securePwd = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name $env:RDP_USER -Password $securePwd -AccountNeverExpires -ErrorAction SilentlyContinue
          Add-LocalGroupMember -Group "Administrators" -Member $env:RDP_USER -ErrorAction SilentlyContinue
          Write-Host "✅ RDP User created! Username: $env:RDP_USER | Password: $password"

      # 修复核心：手动指定路径+验证+强制启动服务
      - name: Install Tailscale via winget (Fix Path)
        shell: pwsh
        run: |
          Write-Host "=== Installing Tailscale via winget ==="
          # 1. 强制卸载旧版本（避免冲突）
          winget uninstall --id Tailscale.Tailscale --silent -ErrorAction SilentlyContinue
          Start-Sleep -Seconds 5
          
          # 2. 静默安装最新版本
          winget install --id Tailscale.Tailscale `
            --silent `
            --accept-package-agreements `
            --accept-source-agreements `
            --disable-interactivity
          
          # 3. 等待安装完成+手动验证路径
          Start-Sleep -Seconds 15
          if (Test-Path $env:TAILSCALE_PATH) {
            Write-Host "✅ Tailscale installed at: $env:TAILSCALE_PATH"
            # 将路径加入临时环境变量
            $env:PATH += ";C:\Program Files\Tailscale"
          } else {
            # 备用路径（32位系统）
            $altPath = "C:\Program Files (x86)\Tailscale\tailscale.exe"
            if (Test-Path $altPath) {
              Write-Host "✅ Tailscale found at alternative path: $altPath"
              $env:TAILSCALE_PATH = $altPath
              $env:PATH += ";C:\Program Files (x86)\Tailscale"
            } else {
              Write-Error "❌ Tailscale executable not found in default paths"
              exit 1
            }
          }

          # 4. 强制启动Tailscale服务
          Write-Host "=== Starting Tailscale Service ==="
          Start-Service -Name tailscaled -ErrorAction SilentlyContinue
          if (-not (Get-Service -Name tailscaled -ErrorAction SilentlyContinue)) {
            # 手动启动服务文件
            Start-Process -FilePath $env:TAILSCALE_PATH -ArgumentList "service install" -Wait -NoNewWindow
            Start-Service -Name tailscaled -ErrorAction SilentlyContinue
          }
          Write-Host "✅ Tailscale service started"

      - name: Connect to Tailscale Network
        shell: pwsh
        env:
          TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          Write-Host "=== Connecting to Tailscale ==="
          if (-not $env:TAILSCALE_AUTH_KEY) {
            Write-Error "❌ TAILSCALE_AUTH_KEY is missing!"
            exit 1
          }
          
          # 使用完整路径执行tailscale命令（核心修复）
          & $env:TAILSCALE_PATH up --authkey $env:TAILSCALE_AUTH_KEY `
            --hostname "Windows-RDP-EN" `
            --timeout 60s `
            --accept-dns=false `
            --accept-routes=false
          
          # 获取IP（兼容两种路径）
          $tsIP = & $env:TAILSCALE_PATH ip -4
          if (-not $tsIP -or $tsIP -eq "") {
            Write-Error "❌ Failed to get Tailscale IP address"
            exit 1
          }
          
          echo "TAILSCALE_IP=$tsIP" | Out-File -FilePath $env:GITHUB_ENV -Append
          Write-Host "✅ Tailscale connected! Access IP: $tsIP"

      - name: Show RDP Info & Keep Alive
        shell: pwsh
        run: |
          Write-Host "`n========================================"
          Write-Host "📡 Windows RDP Access Information"
          Write-Host "========================================"
          Write-Host "🔌 Tailscale IP (RDP Address): $env:TAILSCALE_IP"
          Write-Host "👤 RDP Username: $env:RDP_USER"
          Write-Host "🔑 RDP Password: $env:RDP_PWD"
          Write-Host "⏰ Expiration Time: 5 Hours"
          Write-Host "========================================`n"
          
          $startTime = Get-Date
          while ($true) {
            $elapsed = (Get-Date) - $startTime
            if ($elapsed.TotalSeconds -ge $env:RUN_DURATION) {
              Write-Host "⏰ 5 Hours expired! Workflow terminating..."
              exit 0
            }
            $elapsedMins = [math]::Round($elapsed.TotalMinutes)
            $remainingMins = [math]::Round(($env:RUN_DURATION - $elapsed.TotalSeconds)/60)
            Write-Host "[$(Get-Date)] Running - Elapsed: $elapsedMins mins | Remaining: $remainingMins mins"
            Start-Sleep -Seconds 300
          }
